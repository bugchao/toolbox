import React from 'react'

/**
 * Phase 1 占位 shell。完整 UI（侧边栏 + 编辑器 + 预览 + 设置面板）
 * 将在 Section 7 落地，本 commit 仅交付域模型 + 持久化 + 适配器骨架。
 */
const App: React.FC = () => (
  <main style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
    <h1>Diagram Workbench</h1>
    <p>WIP shell — domain model, IndexedDB persistence, Mermaid adapter ready.</p>
  </main>
)

export default App
