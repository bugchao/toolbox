{
  "name": "@toolbox/api-gateway",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "dev": "node --watch src/main.js",
    "build": "rm -rf dist && mkdir -p dist && cp -R src/. dist/",
    "start": "node src/main.js",
    "start:dist": "node dist/main.js"
  },
  "dependencies": {
    "@toolbox/legacy-tools-service": "workspace:*",
    "@toolbox/service-core": "workspace:*",
    "express": "^4.18.2"
  }
}
